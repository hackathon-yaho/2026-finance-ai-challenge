# `gcloud run deploy --source .`가 업로드하지 않을 파일.
# 이 파일이 없으면 gcloud가 .gitignore를 대신 참고하는데, 그러면 규칙이
# 두 곳으로 갈라지므로 명시해 둔다.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.env
.git/
.gitignore

tests/
docs/
demo/
requirements-dev.txt
README.md
