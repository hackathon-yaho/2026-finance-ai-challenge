# 실제 값은 `python scripts/set_key.py`로 등록하세요 — 이 파일을 복사해 쓰지 마세요.
# .env는 .gitignore에 있어 저장소에 올라가지 않습니다.

# LLM API 키 — 공급자에 따라 둘 중 하나만 있으면 됩니다 (공급자 미확정, 2026-08-25)
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# 백엔드와 공유하는 내부 API 토큰 (32자 이상 랜덤, 백엔드가 생성해 팀 채널로 공유)
INTERNAL_TOKEN=
