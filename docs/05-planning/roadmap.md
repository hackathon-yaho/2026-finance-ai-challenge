# 로드맵

> **수정 기록 (2026-08-25 ②, 백엔드)** — 근거: 프론트 회신 5건
> - **"회신 대기 중" 절을 해소 상태로 교체.** 프론트 4건이 전부 도착해 **AI·프론트 양쪽 블로커가 모두 풀렸습니다** — 남은 것은 구현뿐입니다
> - 백엔드 확정 작업 **10~19번 신설.** 그중 **11번(`필수증빙누락` 정의)이 가장 급합니다** — 종전 정의 그대로 짜면 TC-21·TC-22가 깨집니다
> - **`/api/draft/revise`를 8/29~8/31로 확정** (프론트가 일정을 당겼고 백엔드가 수용)
> - 프론트 배포 완료(8/25, 기한 9/5보다 앞당김) 반영, 9/6 최종 점검에 **PDF 한글 렌더 확인** 추가
> - 회신 위치 표기 정정 — 배포 도메인 회신은 `response/backend/`에 옵니다(**폴더 이름 = 읽을 사람**)

> **수정 기록 (2026-08-25, 백엔드)**
> - **"AI 회신 대기 항목" 절을 해소 상태로 갱신.** 4건 전부 회신 완료 — Phase 3·5의 AI 쪽 블로커가 남아 있지 않습니다. AI-server는 코드까지 구현돼 저장소에 올라와 있습니다(`../../ai-server/`)
> - 그 자리에 **백엔드 후속 작업 4건**(토큰 생성·데모 세트 복사·`AI_SERVER_URL` 수신·데모 이미지 확정)을 시점과 함께 정리
> - 리스크 레지스터의 미회신 항목을 **프론트 4건**으로 갱신

> **수정 기록 (2026-08-24 저녁, 백엔드)**
> - **"AI 회신 대기 항목"** 절 신설. Phase 3·6을 막고 있는 AI 회신 대기 3건이 이 문서 어디에도 안 보여서, 로드맵만 보고 일정을 짜면 이 블로커를 놓칠 수 있었습니다
> - "코드 착수 시 반영할 계약 확정값"에 오늘 오후 새로 보낸 요청 4건(증빙 구조·법정 서식·고지 문구·구매자 송금인 대조) 반영 — 전부 **회신 대기 중**이라 착수 시점을 아직 못 박습니다

> **수정 기록 (2026-08-24, 백엔드)**
> - **"코드 착수 시 반영할 계약 확정값"** 절 신설. 2026-08-24 프론트 회신(`../response/backend/pdf-ownership-and-open-contracts.md`)으로 확정돼 문서에는 반영했지만 **아직 구현하지 않은** 항목을 담당·착수 시점별로 모았습니다. 문서 정리 단계에서는 코드를 건드리지 않고, 해당 기간에 이 표를 체크리스트로 씁니다

> 출처: `../00-context/prd.md` §12.2~12.4, `../00-context/spec.md` 부록.

## 일정

| 기간 | 마일스톤 | 산출물 |
| --- | --- | --- |
| 8/22~8/24 (3일) | 킥오프 · 스켈레톤 | Spring Boot 프로젝트(백엔드/AI-server 각각), Render 배포 성공, Supabase 연결, 5단계 라우팅 껍데기 |
| 8/25~8/28 (4일) | 코어 기능 | FR-010~014 문진·기한안내, FR-020~022 판독·타임라인, FR-027 마스킹, FR-028 카드 확인, FR-030~035 준비도 점검 엔진 |
| 8/29~8/31 (3일) | 문서 생성 | FR-040~047 소명서·문장근거연결·제출패키지, FR-023~025 대체 증빙·공백 지목 |
| 9/1~9/2 (2일) | 완성도 | FR-024 협박 대응, FR-029 품질 검사, FR-050~052 접수 안내, 반응형·접근성, 오프라인 데모 모드 |
| 9/3~9/4 (2일) | **문서 집중** | 기획서 PDF, 기능명세서 PDF 초안 완성 (이 기간 코딩 금지) |
| 9/5 (1일) | **인프라 확정** | 백엔드 Render Free 배포 + cron-job.org 킵얼라이브, AI-server Cloud Run 배포, 헬스체크, 부하 확인 |
| 9/6 (1일) | 최종 점검 | 전체 플로우 3회 완주, 모바일 확인, 문서 최종본, **생성된 PDF를 실제로 열어 한글이 정상 렌더되는지 확인** |
| 9/7 오전 | **제출** | 10:00 마감 — 실제 제출은 9/6 밤 완료 권장 |

## 스코프 컷 우선순위

일정이 밀릴 때 버리는 순서. **상세 컷 순서는 `../00-context/spec.md` 부록을 단일 기준으로 삼습니다.** 절대 버리지 않는 핵심(우선순위 순):

1. **FR-020~022 판독·타임라인** — AI 필요성 논증의 핵심
2. **FR-028 추출 결과 확인** — AI 신뢰성의 최소 조건
3. **FR-032~033 증빙 점검·준비도 산출** — 결정적 규칙 구현의 핵심
4. **FR-040 소명서 생성** — 데모의 절정
5. **FR-046 문장-근거 연결** — 이 서비스의 최대 차별점
6. **FR-024 협박 대응** — 이 서비스만의 차별점

FR-043(텍스트 면 PDF)은 FR-047(제출 패키지)의 입력이 되므로 컷 대상이 아닙니다. 화면 인쇄(F8-02)는 PDF 생성 실패 시의 비상 경로로만 유지합니다.

## 코드 착수 시 반영할 계약 확정값

문서로 확정만 해두고 **아직 구현하지 않은** 항목입니다. 문서 정리 단계에서는 손대지 않고, 해당 기간에 코드 작업을 시작할 때 이 표를 체크리스트로 씁니다. 각 항목의 근거와 상세는 계약 문서에 있으니 여기에 스펙을 복사하지 않습니다.

### 백엔드 (A)

| # | 항목 | 착수 시점 | 단일 출처 |
| --- | --- | --- | --- |
| 1 | `spring.servlet.multipart.max-file-size` / `max-request-size` = **10MB** | 8/25~8/28 (판독 착수 전) | `../02-architecture/api-contract.md` §업로드 크기 상한 |
| 2 | CORS 허용 origin `http://localhost:5173` **+ 프론트 프로덕션 도메인**(10번) + 허용 헤더 `X-Session-Hash` (**프리뷰 와일드카드 금지**) | 8/25~8/28 (프론트 연동 착수 전) | 같은 문서 §CORS 허용 origin·헤더 |
| 3 | 파일 검증 4종을 **서버에서도** 수행 — 특히 **매직바이트** | 8/25~8/28 | `../00-context/spec.md` F3-02 검증 주체 |
| 4 | `POST /api/package/text` — **11필드** 전부 선택, 공란 허용, `INVALID_FORM_FIELD` 형식 검증 (17번으로 확장) | 8/29~8/31 (문서 생성) | `api-contract.md` §`/api/package/text` |
| 5 | 같은 요청의 **바디를 로그에 남기지 않도록** 접근 로그 필터 설정 | 8/29~8/31 (4번과 같이) | `../03-infra-ops/privacy-and-safety.md` |
| 6 | `GET /api/timeline`에 `mergeCandidates` 산출 (자동 병합 금지) | 8/25~8/28 | `api-contract.md` §`/api/timeline` 응답 |
| 7 | `POST /api/timeline/merge` 승인 엔드포인트 | **스코프 컷 대상** (컷 순서 4번) | `spec.md` F5-02 |
| 8 | `deadline.notice`를 공고일 미입력 시에도 **항상** 채워 내려보내기 | 8/25~8/28 | `api-contract.md` §`/api/intake` 응답 |
| 9 | Render 프록시 요청 바디 상한이 10MB를 통과하는지 확인 | 9/5 (인프라 확정) | `../03-infra-ops/deployment-and-uptime.md` |

### 프론트 (C)

| # | 항목 | 착수 시점 |
| --- | --- | --- |
| 1 | 서식 **11필드** 폼 — **전부 선택**, 필수 표시 금지 (길이 100자 / `birthDate` 형식만 검증). "신청인과 동일" 체크박스 기본 체크 | 8/29~8/31 |
| 2 | `/api/package/text`를 `POST` + 바디로 호출 | 8/29~8/31 |
| 3 | `/api/evidence` 호출을 **동시 4개**로 제한 (10장이면 4→4→2) | 8/25~8/28 |
| 4 | ~~배포 도메인 확정 → CORS 등록 요청~~ | **✅ 2026-08-25 완료** (`../response/backend/deployment-domain.md`) |
| 5 | F2-01a 거래 방식 문항, S04-2 미리보기·문장 수정 | 8/29~8/31 |

> **7번(F5-02)을 컷하면** 백엔드는 `mergeCandidates`를 항상 빈 배열로 내리고 승인 엔드포인트를 만들지 않습니다. 프론트는 빈 배열이면 후보 UI를 렌더하지 않으므로 양쪽이 서로를 기다리지 않습니다. **컷 결정이 나면 `../response/frontend/`에 회신합니다** (폴더 이름 = 이 문서를 읽을 사람 = 프론트).

### 프론트 회신으로 확정된 백엔드 작업 (2026-08-25 — 회신 대기 해소)

**프론트 4건 회신이 전부 도착했습니다.** 종전 "회신 대기 중" 절의 항목이 여기로 옮겨졌습니다. **AI·프론트 양쪽 블로커가 모두 풀려, 남은 것은 구현뿐입니다.**

| # | 항목 | 착수 시점 | 단일 출처 |
| --- | --- | --- | --- |
| 10 | **CORS에 `https://2026-finance-ai-challenge-tau.vercel.app` 등록** (슬래시 없이) | 8/25~8/28 (2번과 같이) | `api-contract.md` §CORS |
| 11 | **`필수증빙누락`을 `whenMissing: "blocks"` 기준으로 구현** — 종전 정의 그대로 짜면 **TC-21·TC-22가 깨집니다** | 8/25~8/28 (Phase 4 착수 전) | `../01-product/reason-type-rules.md` §3-1 |
| 12 | **`checklist` 새 스키마** (`tier`·`fulfillBy`·`whenMissing`·`options` 택일) | 8/25~8/28 | `api-contract.md` §`checklist` 항목 스키마 |
| 13 | **`POST /api/checklist/self-held`** — 자가 진술 세션 보관 (PDF·로그 미기록) | 8/25~8/28 | `api-contract.md` |
| 14 | `/api/intake`에 **`deliveryMethod`** + **F5-03 ① 직거래 예외** | 8/25~8/28 | `spec.md` F2-01a·F5-03 |
| 15 | **`notices` 고정 문구** — "심사 결과 통보"와 "지급정지 해제" 분리 (법 제8조 제2항) | 8/25~8/28 | `spec.md` F6-05 |
| 16 | 구매자–송금인 **대조 + 불일치 안내 문구**를 `checklist[].note` 또는 `notices`로 | 8/25~8/28 | `reason-type-rules.md` §2-1, Phase 4-4 |
| 17 | `/api/package/text` **11필드** + `excludedSentenceIds` | 8/29~8/31 (4번 대체) | `api-contract.md` |
| 18 | **표지 + 면별 항목·정렬 기준**대로 PDF 생성, 부족자료 체크리스트 제외, `{시각}` = 다운로드 시각 | 8/29~8/31 | `spec.md` F8-01 |
| 19 | **`POST /api/draft/revise`** — 수정 문장은 삭제하지 않고 `user_text` + `warning` | **8/29~8/31** (프론트 일정에 맞춤) | `api-contract.md`, `prd.md` FR-045 ③ |

> **11번이 가장 급합니다.** `reason-type-rules.md` §3의 종전 정의("체크리스트에 미보유 항목이 있음")를 그대로 구현하면 **채울 수 없는 항목 때문에 사용자가 영원히 "보완 필요"에 갇힙니다.** Phase 4 착수 전에 §3-1을 읽으세요.
>
> **19번은 프론트가 일정을 당긴 건입니다.** 종전 회신에서 "8/29~8/31에는 필요 없다"고 했던 것을 프론트가 뒤집었고, 백엔드가 **가능**으로 답했습니다. 어려워지면 프론트에 알리면 됩니다 — 프론트는 읽기 전용 + 문장 제외로 먼저 완성해 두고 자유 편집만 9/1~9/2로 되돌립니다.

### 남은 미확정 — 없음

**AI 4건·프론트 4건 회신이 모두 완료됐습니다.** 백엔드를 막는 회신 대기 항목은 없습니다.

## AI 회신 대기 항목 — ✅ **2026-08-25 전부 해소**

Phase 3·6을 막고 있던 3건에 회신이 왔고, AI가 보낸 신규 요청 1건도 같은 날 회신 완료했습니다. **Phase 3·5의 AI 쪽 블로커는 남아 있지 않습니다.**

| 항목 | 결론 | 회신 |
| --- | --- | --- |
| 이미지 전달 방식 | **A 계열 raw body** (멀티파트 봉투 없음, base64 기각) | `../response/backend/image-transfer-and-internal-auth.md` |
| 카드별 `source_type` | **이벤트 단위**로 확정. `unknown`은 tie-break 최하위 | `../response/backend/card-source-type.md` |
| 데모 응답 세트 JSON | **v1 조기 납품 완료** — `../../ai-server/demo/` (9개 파일) | `../response/backend/demo-response-set.md` |
| 이름 필드 (`counterparty_name`/`payer_name`) | 필드 수용 + **원문 추출**로 확정 (부분 마스킹 기각) | `../response/backend/payer-name-extraction.md` |
| (AI → 백엔드) `/internal/draft` `intake` | **원안 수용**, 계약 반영 완료 | `../response/ai/draft-intake-input.md` |

**AI-server는 코드까지 구현돼 저장소에 올라와 있습니다** (`ai-server/`, FastAPI + 테스트 3종). 백엔드가 이어서 할 것:

| 항목 | 시점 |
| --- | --- |
| `INTERNAL_TOKEN` 생성·팀 채널 공유 (32자 이상 랜덤) | Phase 3 착수 전 |
| `ai-server/demo/` v1 → `backend/src/main/resources/demo/` 복사 | Phase 3 착수 시 (단위 테스트 픽스처로도 사용) |
| `AI_SERVER_URL` 수신 (AI가 8/26 배포 후 공유) | Phase 6 킵얼라이브 |
| 데모 이미지 4장 확정 → AI가 데모 세트 v2 재생성 | 9/1~9/2 리허설 안건 |

> **2026-08-25 ② 기준 — 회신 대기 항목은 없습니다.** 프론트 5건도 전부 도착했고, §1-A(F3-06 마스킹 안내 문구)는 **구현 완료**됐습니다. 위 "프론트 회신으로 확정된 백엔드 작업" 절이 남은 전부입니다.

## 리스크 레지스터

| 리스크 | 확률 | 영향 | 대응 |
| --- | --- | --- | --- |
| Render 스핀다운으로 URL 접근 불가 | 중 (제3자 크론 의존, 2026-08-25 ② Free+크론으로 전환) | 치명적 | cron-job.org 10분 주기 + 백업 모니터 이중화 — `../03-infra-ops/deployment-and-uptime.md` §2 |
| Supabase 일시정지 | 중 | 치명적 | 킵얼라이브 cron |
| 이미지 판독 정확도 미달 | 중 | 높음 | 텍스트 입력 대체 경로 (FR-026) |
| 문서 작성 시간 부족 | 높음 | 치명적 | 9/3~9/4 문서 전용일 사수. 코딩 금지 |
| LLM API 비용 초과 | 낮음 | 중 | 이미지 최대 10장 제한, 세션당 호출 상한 |
| 3인 일정 충돌 | 중 | 중 | 매일 15분 스탠드업, 병목 즉시 재분배 |
| 마스킹·문장근거연결 프론트 작업량 초과 | 중 | 높음 | 마스킹은 박스 그리기 방식으로 단순화. FR-046은 문장→이미지 스크롤 이동까지만 구현하고 정밀 좌표 하이라이트는 P1로 분리 |
| 브라우저 새로고침 시 원본 소실 | 높음 | 중 | `beforeunload` 경고 + 재진입 시 "원본이 필요합니다. 다시 올려주세요" 안내. 추출 결과·소명서는 서버 세션에 남으므로 재업로드만 하면 복구 |
| 모바일 메모리 초과로 탭 강제 종료 | 중 | 높음 | 업로드 시 장변 1600px 리사이즈. 10장 상한 유지. 사용 후 blob URL revoke |
| 클라이언트 PDF 병합 실패 | 중 | 중 | 텍스트 PDF 다운로드 + 원본 이미지 개별 저장 안내로 폴백 |
| **미회신 요청 누적으로 Phase 4 착수 지연** | 중 | 높음 | **2026-08-25 기준 AI 4건 전부 해소, 프론트 4건 대기 중.** Phase 3(`AiClient`)는 블로커가 풀려 바로 착수 가능. 남은 리스크는 Phase 4(`ReadinessService`)의 체크리스트 자료구조 — 프론트 `evidence-structure-revision.md` §2 회신에 걸려 있다. **확정된 규칙만으로 먼저 짜고 증분 반영**하는 방침 유지 |

## 매일 스탠드업 권장 형식 (15분)

1. 어제 완료한 FR/NFR/F ID
2. 오늘 할 FR/NFR/F ID
3. 막힌 것 — 특히 `../02-architecture/api-contract.md`나 `../01-product/reason-type-rules.md`에 대한 해석 차이는 그 자리에서 문서를 고쳐 합의한다. 백엔드(A)와 AI(B)는 같은 저장소에 코드를 올리므로, 패키지 구조 충돌도 그 자리에서 조율한다
