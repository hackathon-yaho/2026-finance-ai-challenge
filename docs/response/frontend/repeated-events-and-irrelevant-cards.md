# 회신: 반복 이벤트·무관 카드·null 날짜 게이팅

> 원본 요청: `../../request/backend/repeated-events-and-irrelevant-cards.md` (프론트 · 2026-08-26)
>
> **한 요청에 두 사람이 답했습니다.** 요청이 §2는 백엔드, §3·§4·§7은 AI 소관으로 걸쳐 있어
> 각자 이 파일에 회신했습니다. 아래는 **백엔드 회신 → AI 회신** 순입니다.
>
> (백엔드 회신이 먼저 있었는데 AI 회신이 같은 경로에 덮어써서 한 번 사라졌던 것을
> 프론트가 되살려 합쳤습니다. 내용은 양쪽 원문 그대로입니다.)

---

# [백엔드 → 프론트] 회신 (2026-08-26)

## §1·§4 — 반복 이벤트 묶기는 AI 담당 몫입니다, A안에 동의합니다

**A안(AI 추출 단계에서 묶기)을 지지합니다.** B안(백엔드가 결정적 규칙으로 묶기)은 §3(타임아웃)을 못 풉니다 — 카드가 이미 12장 만들어진 뒤에 백엔드가 묶어봤자 AI-server의 출력 토큰과 지연은 그대로입니다. C안(프론트 화면에서만 묶기)에 반대하시는 이유(미리보기-제출본 불일치, F4-06 확인 단위 붕괴)에도 동의합니다 — 저희도 `evt_intake_when`·3면 미확인 카드에서 같은 문제를 두 번 겪었습니다.

**백엔드가 할 일**: AI 담당이 `recurrence` 필드(또는 그에 준하는 표현)를 카드 스키마에 신설하기로 확정하면, `internal-api-contract.md`·`api-contract.md`에 그 스키마를 반영하고 `ExtractedEvent`에 필드를 추가하겠습니다. **지금 당장은 없습니다** — AI 담당의 설계가 먼저입니다.

## §2 — 무관 거래 필터링은 하지 않습니다 (현행 유지)

**추출 단계에서도, 4면 구성 단계에서도 거르지 않기로 결정했습니다.**

이유가 하나 더 있습니다 — **급여·월세·정기결제 카드는 실은 "무관한 정보"가 아닙니다.** `TimelineServiceImpl`의 F5-04 공백 탐지(`no_life_activity`)가 정확히 이런 카드들의 존재로 "생계 흔적 없음" 공백을 안 띄웁니다. 즉 이 카드들은 이의제기 사유와는 무관해 보여도 **"이 계좌를 생활에 쓴다"는 별개의 소명 요건을 채우는 증거**입니다. 추출 단계에서 "무관하다"고 걸러버리면 생계 흔적 공백을 메울 자료가 통째로 사라집니다. 4면 구성 단계에서 거르는 것도 반대입니다 — F8-01이 4면을 "확인된 증거 카드 전체의 목차"(5면과 1:1 대응)로 정의해뒀는데, 여기서 관련성을 판단하려면 백엔드가 애매한 기준을 새로 만들어야 합니다.

**결론: 기존 장치(F4-06, `/api/evidence/confirm`의 `confirmed:false` 카드 삭제)로 사용자가 직접 뺍니다.** 백엔드 변경은 없습니다. "일곱 번 눌러야 하는" UX 부담은 실제 문제이지만, 화면에서 일괄 선택/제외 같은 걸로 푸는 게 맞다고 봅니다 — 이건 프론트 쪽 판단에 맡기겠습니다.

## §3 — 타임아웃 실측 공유 감사합니다

AI 담당에게 그대로 전달 부탁드립니다. §4(A안)가 확정되면 카드 수 자체가 줄어 자연히 풀릴 문제로 보고 있습니다.

## §7 — `occurred_at == null`도 게이팅 대상으로 확정, 구현 완료

**A안(사용자가 채우게 하고, 채우기 전엔 막는다) 그대로 갔습니다.**

`EvidenceServiceImpl.hasBlockingUnconfirmedCards`에 조건을 추가해 `occurred_at == null`인 `pending` 카드도 `low` 신뢰도와 동급으로 차단합니다. `amount == null`은 그대로 예외로 남깁니다(대화 캡처에 금액이 없는 것은 정상이라는 기존 원칙 유지) — `occurred_at`만 다르게 취급합니다. `/api/readiness`·`/api/draft` 둘 다 이 메서드를 공유해서 자동으로 같이 움직입니다.

**정확히 알아두셔야 할 것: 이 수정은 "시각 미상"을 없애주지 않습니다.** 목적은 사용자가 그 카드를 한 번은 보고 넘어가게 강제하는 것뿐입니다 — 그냥 "확인"만 눌러도 게이팅은 풀리고, 그 경우 3면에는 여전히 "시각 미상"이 남습니다. 날짜를 실제로 채우는 것은 여전히 사용자 선택입니다. 연도를 추측해서 채우는 방식(B안)은 채택하지 않았습니다 — 틀린 날짜가 그럴듯해 보여서 사용자가 의심 없이 확인해버릴 위험이 더 크다고 판단했습니다.

**프론트가 맞춰야 하는 부분**: 요청하신 대로 F4-06 화면 차단 조건도 같은 규칙(`occurred_at == null`이면 차단)으로 맞춰주셔야 두 방어선이 짝이 맞습니다. 지금 프론트 차단 조건이 `low` 신뢰도만 보고 있다면 `null`도 추가해 주세요.

계약 변경 반영: `api-contract.md` v1.15, `internal-api-contract.md`, `prd.md` FR-028.

## 후속 작업

| # | 항목 | 담당 |
| --- | --- | --- |
| 1 | §4 반복 이벤트 스키마 설계 | AI (백엔드는 확정되면 계약 반영) |
| 2 | §7 프론트 F4-06 차단 조건에 `occurred_at == null` 추가 | 프론트 |

§2·§7 코드 변경은 완료했고 테스트로 회귀 방지 걸어뒀습니다.

---

# [AI → 프론트] 회신: §4 반복 이벤트 묶기 — **A안으로 구현했습니다**

- 원본 요청: `../../request/backend/repeated-events-and-irrelevant-cards.md` (프론트 · 2026-08-26)
- 회신: AI · 2026-08-26 (§3·§4·§7 = AI 소관분)

## 결론 — A안(추출 단계에서 묶기) 채택. 계약 반영·구현·실측까지 마쳤습니다

§4의 판단이 맞습니다. **B안은 §3이 안 풀리고, C안은 PDF와 화면이 갈립니다.** 두 근거 모두 그대로 받아들였습니다.

## 1. 계약: 카드에 `recurrence` 신설

`../../02-architecture/internal-api-contract.md` "recurrence" 절에 넣었습니다. 반복이 아니면 `null`이고, 대부분의 카드는 `null`입니다.

```json
"recurrence": { "count": 12, "period": "monthly",
                "first": "2026-01-15T09:00:00+09:00",
                "last":  "2026-12-15T09:00:00+09:00" }
```

| 규칙 | 내용 |
| --- | --- |
| `amount`는 **1회분** | 총액이 아닙니다. 총액을 넣으면 백엔드 금액 대조가 어긋납니다 |
| `occurred_at`은 **`first`** | 타임라인 정렬 기준 |
| **`count`는 AI-server가 계산** | LLM이 센 값을 쓰지 않습니다 — 아래 §2 |

§5에서 말씀하신 "개별 날짜를 버리지 않는다"는 `first`/`last`/`count`로 지켜집니다.

## 2. "12회"를 LLM에게 세게 하지 않았습니다

**LLM은 개별 발생 일시를 나열만 하고, 횟수는 코드가 셉니다.** `count`가 틀리면 **은행에 가는 문서의 숫자가 틀리는 것**이라, 확률적인 값을 그대로 쓸 수 없었습니다.

- LLM 출력: `{ period, occurrences: ["2026-01-15T09:00:00+09:00", ...] }`
- AI-server: 파싱 가능한 일시만 골라 정렬 → `count = 개수`, `first = 첫째`, `last = 마지막`
- 파싱 가능한 일시가 **2개 미만이면 반복으로 보지 않습니다** (근거 부족)

소명서 문장의 "12회"도 사실검증기가 대조합니다 — 횟수를 부풀린 문장은 삭제됩니다(회귀 테스트 포함).

## 3. 같은 날 반복은 묶지 않습니다 — 실측에서 잡은 부작용

구현 직후 평가 세트에서 **의도하지 않은 묶임**이 나왔습니다. `ev-bank-02`(같은 날 1분 간격 입금 3건)가 카드 1장으로 묶였습니다. 규칙("같은 금액·같은 내용·규칙적 간격")에는 들어맞지만, **정기 거래가 아니라 개별 입금**이고 소명에서는 각각이 사실입니다.

**반복 주기가 최소 하루 이상일 때만 묶도록 코드에서 막았습니다.** 프롬프트에도 같은 경계를 적었지만, 프롬프트 준수는 확률적이라 코드 가드를 신뢰 지점으로 뒀습니다.

## 4. §3 지연 — 함께 풀렸습니다

말씀하신 대로 출력이 12건 → 1건이 되면서 지연이 같이 내려갔습니다.

| | 묶기 전 | 묶은 후 |
| --- | --- | --- |
| 자동이체 12개월 캡처 | **10.4초** | **5.1~5.4초** |
| 전체 p95 (28건) | 6.2초 | **5.8~6.8초** |

프론트가 실측하신 15.1초(→`504`)보다 제 쪽 수치가 낮게 나온 건 합성 캡처라 그렇습니다 — **실제 캡처가 더 무겁습니다.** 리허설에서 실제 데모 이미지로 다시 재겠습니다.

## 5. 알려주신 두 조건을 평가 세트에 넣었습니다

**이게 이번 회신에서 제일 고마운 부분입니다.** 제 평가 세트는 제가 만든 합성 이미지라 **제가 생각한 조건까지만** 담고 있었습니다.

| 알려주신 조건 | 제 세트가 못 잡은 이유 |
| --- | --- |
| 한 장에 이벤트 12건 | 제 이미지는 **한 장에 최대 3건**이었습니다. 즉 "p95 5.9초 달성"은 1~3건일 때의 숫자였습니다 |
| 은행 캡처가 `08.19`로 연도 없음 | 제 이미지는 **전부 연도가 있었습니다** |

`ev-many-01`·`ev-noyear-01`로 넣어 회귀로 지킵니다. 세트는 `../../../ai-server/evals/`에 있고 **누구나 열어서 검증하실 수 있습니다** — 기대값(`cases.py`)과 채점 규칙(`runner.py`)까지 전부 저장소에 있습니다.

## 6. §7 확인 — 연도 없는 캡처

`ev-noyear-01`(`08.19  10:07` 3행)로 재봤더니 **연도를 지어내지 않고 `occurred_at: null`을 냈습니다.** 말씀하신 대로 AI 품질 문제가 아니라 원칙대로 동작한 것입니다.

**A안(게이팅)에 동의합니다.** 백엔드가 이미 구현하셨다고 하니 AI 쪽에서 추가할 것은 없습니다. B안(연도 힌트)은 **틀린 연도가 그럴듯하게 은행에 가는** 위험이 있어 저도 반대합니다.

## 7. 프론트가 할 것

계약이 확정됐으니 반복 표기를 렌더해 주시면 됩니다.

- **카드**: `recurrence`가 있으면 배지 (예: "매월 · 12회")
- **3면·4면**: 각 한 줄. §5에서 말씀하신 "매월 15일 65,890원 · 12회" 형태가 정확합니다
- `recurrence`가 `null`인 카드가 대부분이니 **없을 때가 기본**입니다

§2(무관 거래)는 백엔드가 현행 유지로 결정하셨고 AI 쪽 변경 없습니다.
