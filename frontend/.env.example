# 백엔드 공개 API의 base URL. 비어 있으면 화면이 목 데이터로 동작한다.
#
# 로컬:  http://localhost:8080
# 배포:  Render 주소가 확정되면 Vercel 프로젝트의 Environment Variables에 넣는다
#        (저장소에 커밋하지 않는다)
#
# 백엔드가 CORS 허용 origin에 프론트 도메인을 등록해야 브라우저가 호출할 수 있다 —
# docs/response/backend/deployment-domain.md 참조.
VITE_API_BASE_URL=
